numpy==1.14.0 # already with anaconda
scipy==1.0.0 # already with anaconda
scikit-learn>=0.18 # already with anaconda
spectral>=0.17
matplotlib>=2.0 # already with anaconda
cvxopt >= 0.18
jupyter>=1.0.0 # already with anaconda
tabulate>=0.7.5
pandas>=0.22.0 # already with anaconda
plotnine>=0.3.0
lightgbm==2.1.2
xgboost==0.72.1